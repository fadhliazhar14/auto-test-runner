# Database Configuration for BDD Test Runner
# MySQL Database Settings

# Database Connection Settings
db.url=jdbc:mysql://localhost:3306/bdd_test_runner?useSSL=false&allowPublicKeyRetrieval=true&createDatabaseIfNotExist=true&serverTimezone=UTC
db.username=root
db.password=Kamikaze11
db.driver=com.mysql.cj.jdbc.Driver

# Connection Pool Settings (HikariCP)
db.pool.max_size=10
db.pool.min_idle=5
db.pool.connection_timeout=30000
db.pool.idle_timeout=600000
db.pool.max_lifetime=1800000

# Application Settings
app.temp_features_dir=temp-features
app.logs_dir=logs
app.screenshots_dir=screenshots